sampleIT is my database of sample information, photos, videos etc.

In order to publish this project without having to publish my whole database, I have created an interface that looks like the sampleIT interface, but uses locally cached files rather than the live database.
